<UserControl x:Class="EConstruction.Views.IntegrationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F0FFF0">
        <!-- Медовий роса фон -->
        <TextBlock Text="Модуль Інтеграції"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="36" FontWeight="Bold" Foreground="#333333"/>
        <TextBlock Text="Налаштування зв’язку з ERP (Business Central), BIM (ArchiCAD)."
                   HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,50"
                   FontSize="20" Foreground="#666666"/>
    </Grid>
</UserControl>
