using System.Windows;
using EConstruction.ViewModels;

namespace EConstruction
{
    /// <summary>
    /// Логіка взаємодії для MainWindow.xaml
    /// </summary>
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
            // DataContext встановлюється в XAML
        }
    }
}
